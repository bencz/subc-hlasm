2025-12-23 Implemented char* pointer initialization with string literals (C89):
           - char *str = "hello"; now works correctly
           - const char *str = "hello"; also supported
           - String literal is placed in data section with label
           - Pointer is initialized with reference to label
           - Added genstrlit() and gendeflabel() in gen.c
           - Modified declarator() in decl.c to accept STRLIT for CHARPTR
           - Modified defglob() in sym.c to emit label reference
           - Works on all targets: i386, x86-64, AArch64, ARM, 8086
2025-12-23 Implemented __FILE__, __LINE__, __DATE__, __TIME__ predefined macros:
           - Added special_macro() function in scan.c
           - __FILE__ returns current source filename as string
           - __LINE__ returns current line number
           - __DATE__ returns compilation date as "Mmm dd yyyy"
           - __TIME__ returns compilation time as "hh:mm:ss"
           - Test: t_predefined_special.c
           - 25 preprocessor tests passing
2025-12-23 Implemented stringification (#), token pasting (##), line continuation:
           - Stringification operator (#) in function-like macros
           - Token pasting operator (##) for identifier concatenation
           - Line continuation with backslash-newline in macro definitions
           - Modified expand_funclike_macro() in scan.c for # and ##
           - Modified getln() in pp_main.c for line continuation
           - Added __STDC__ predefined macro (C89 compliance)
           - Tests: t_stringify.c, t_token_paste.c, t_line_cont.c
           - 24 preprocessor tests passing
2025-12-23 Refactored preprocessor into modular structure (src/prep/):
           - prep.h: Main header with all declarations
           - pp_expr.c: Expression evaluator for #if/#elif (~500 lines)
           - pp_macro.c: #define/#undef handling (~160 lines)
           - pp_cond.c: Conditional compilation (#if/#ifdef/etc) (~110 lines)
           - pp_include.c: #include processing (~80 lines)
           - pp_main.c: Main entry points (preproc, playmac, etc) (~90 lines)
           - Removed monolithic prep.c (~984 lines)
           - Updated Makefile with PREP_SRC variable
           - All 21 preprocessor tests passing
2025-12-23 Implemented function-like macros (parameterized macros):
           - #define MACRO(params) body syntax now supported
           - Up to 16 parameters per macro (MAXMACPARAMS in defs.h)
           - Argument parsing with proper parenthesis balancing
           - Parameter substitution in macro body
           - Works with expressions: #define MAX(a,b) ((a)>(b)?(a):(b))
           - Added MAXMACPARAMS and MAXMACARGLEN to defs.h
           - Modified defmac() in prep.c to parse parameters
           - Added expand_funclike_macro() in scan.c for expansion
           - Test: t_macro_func.c in tests/preproc_suite/
2025-12-23 Implemented #if, #elif, and defined() for C89 preprocessor compliance:
           - Full constant expression evaluator in prep.c
           - Supports: integers (decimal, hex, octal), character constants
           - Operators: +, -, *, /, %, <<, >>, <, <=, >, >=, ==, !=
           - Bitwise: &, |, ^, ~
           - Logical: &&, ||, !
           - Ternary: ? :
           - defined(MACRO) and defined MACRO syntax
           - Macro expansion in expressions (e.g., #if VALUE == 2)
           - Proper #elif chaining with #else support
           - New tokens: P_IF, P_ELIF, P_DEFINED in tokens.h
           - Added 8 test files in tests/preproc_suite/ for expression evaluator
2025-12-23 Added predefined macros system for cross-compilation:
           - New struct cg_predef_macro in cgtarget.h for macro definitions
           - OS-specific macros in cg_os_configs.c:
             __linux__, __FreeBSD__, __NetBSD__, __OpenBSD__,
             __APPLE__, __MACH__, _WIN32, __DOS__, __MVS__, __unix__, __ELF__
           - Architecture-specific macros in cg_*.c:
             __i386__, __x86_64__, __amd64__, __aarch64__, __arm64__,
             __arm__, __8086__
           - Dynamic macros generated from cg_arch values:
             __SIZEOF_CHAR__, __SIZEOF_SHORT__, __SIZEOF_INT__,
             __SIZEOF_LONG__, __SIZEOF_POINTER__, __SIZEOF_FLOAT__,
             __SIZEOF_DOUBLE__, __SIZEOF_SIZE_T__, __SIZEOF_PTRDIFF_T__
           - Endianness macros: __LITTLE_ENDIAN__, __BIG_ENDIAN__,
             __BYTE_ORDER__, __ORDER_LITTLE_ENDIAN__, __ORDER_BIG_ENDIAN__
           - Data model macros: __LP64__, __ILP32__
           - All predefined macros can be overridden via -D flag
           - Updated misc.c init() with register_arch_derived_macros()
2025-12-23 Added preprocessor test suite in tests/preproc_suite/:
           - Tests for predefined macros (__SUBC__, arch, OS)
           - Tests for #define, #undef, #ifdef, #ifndef, #else, #endif
           - Tests for nested conditionals and #include
           - run_tests.sh script with target selection (-T flag)
2025-12-23 Added scc_libc field to cg_os_config structure:
           - SubC runtime library (libscc.a) now defined per-OS in cg_os_configs.c
           - Only DOS targets need scc_libc, other targets use system libc directly
           - Removed hardcoded SCCLIBC from defs.h
           - main.c link() now checks if scc_libc is NULL before adding it
2025-12-23 Fixed Darwin linker command for modern macOS:
           - Added SDK library path to ld_cmd for darwin targets
           - ld -o %s -L /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk/usr/lib
           - darwin-aarch64 now fully compiles, assembles, and links executables
2025-12-23 Updated run_tests.sh for full SubC build comparison:
           - Compare mode (-C) now compiles, assembles, links, and runs SubC binaries
           - Compares actual execution output between SubC and native compiler
           - Auto-detects darwin-aarch64 on Apple Silicon
2025-12-23 Fixed functions with many parameters (32+) on AArch64:
           - Stack-passed parameter offsets were calculated with 8-byte spacing
           - But caller pushes with 16-byte alignment (str x0,[sp,#-16]!)
           - Fixed a64_cgparamoffset() to use CG_STACK_ALIGN (16) for stack args
           - Refactored all architectures to use CG->arch macros:
             BPW, CG_STACK_ALIGN, CG_NUM_ARG_REGS, CG_PARAM_OFFSET_BASE
           - Updated cggetframeinfo() and cgparamoffset() in all 5 targets
           - Test: fn33(1..33) now correctly returns 1+33=34
2025-12-23 Fixed variadic function calls on Darwin/AArch64:
           - Darwin AArch64 ABI requires variadic arguments on stack, not registers
           - Added cgpusharg_vararg to vtable for architecture-specific vararg handling
           - Modified tree.c emitargs_abi_ex() to detect variadic functions via Sizes[]
           - printf() and other variadic functions now work correctly on Apple Silicon
           - Other architectures use fallback (NULL) for backward compatibility
2025-12-23 Fixed AArch64/Darwin text relocation errors:
           - Switch statement tables now use PC-relative addressing
           - Global variable access uses adrp+add with @PAGE/@PAGEOFF
           - Static label addresses use adrp+add for Darwin/Mach-O
           - Added switch helper function in prelude for Darwin
           - Modified cgcase() and cgdefl() vtable interface to support
             relative offsets (added table base parameter)
           - Added lgen3() and lgen4() helper functions for label generation
           - darwin-aarch64 target now fully functional with switch statements
2025-12-23 Added AArch64 (ARM64) architecture support:
           - New targets: linux-aarch64, darwin-aarch64
           - Full AAPCS64 calling convention (args in x0-x7)
           - 16-byte stack alignment for Apple Silicon compatibility
           - New cg_frame_info structure for stack frame layout abstraction
           - Added cggetframeinfo(), cgparamoffset(), cglocaloffset(), cgalignlocal()
             to vtable for architecture-specific frame management
           - Updated decl.c to use new frame layout functions
           - OS-aware code generation (OS_TYPE checks for Darwin vs Linux)
           - Literal pool approach for Darwin Mach-O compatibility
           - Implemented for all architectures: x86-64, i386, ARM, 8086, AArch64
2025-12-23 Added comprehensive C89 test suite in tests/c89_suite/:
           - 38 test files covering operators, control flow, types, functions
           - Complex tests: vtables, linked lists, state machines, callbacks
           - run_tests.sh script with compare mode (SubC vs GCC/Clang)
           - Assembly output saved to tests/c89_suite/output/
           - See README_TESTS for full documentation
2025-12-23 Fixed function pointer call via struct member (vtable pattern):
           - OP_CALR in tree.c now properly separates fn ptr eval from args
           - Added cgindw() to dereference the struct member address
           - Enables vtable-based dispatch: vtable.fn(args) now works correctly
2025-12-23 Fixed _va_arg() pointer arithmetic bug in runtime/lib/varargs.c:
           - Old code incorrectly incremented *ap instead of advancing pointer
           - New implementation properly reads value then advances to next arg
2025-12-23 Fixed pointer-to-integer cast issue on 64-bit systems:
           - Removed PTR_INT_CAST macro from src/sym.c
           - Now uses (long) cast directly which works on both 32/64-bit
2025-12-23 Extended function pointer support for bootstrap compatibility:
           - Removed restriction limiting function pointers to int(*)()
           - Now accepts any return type: void(*fn)(void), char*(*fn)(char*)
           - Added skip_fnptr_params() in decl.c to handle typed parameters
           - Modified declarator() to parse function pointers with pointer returns
           - Added skip_fnptr_params_expr() in expr.c for cast expressions
           - Created tests/test_fnptr.c with comprehensive vtable test
2025-12-23 Made compiler limits configurable per architecture:
           - Added fields to cg_arch: namelen, max_case, max_break, max_locinit,
             max_fnargs, nsymbols, poolsize, nodepoolsz, label_prefix
           - Added accessor macros: CG_NAMELEN, CG_MAX_CASE, CG_MAX_BREAK, etc.
           - 8086 now uses reduced limits for 16-bit memory constraints
           - Label prefix now configurable via CG_LABEL_PREFIX (was hardcoded 'L')
           - gen.c updated to use CG_LABEL_PREFIX instead of LPREFIX
2025-12-23 Fixed bootstrap bug: 64-bit integer literals now work correctly
           - Changed Value, Q_val, node.args from int to long
           - Updated mkleaf, mkunop1/2, mkbinop1/2, genlit, cglit signatures
           - Fixes truncation of literals > 2^31 when bootstrapping with gcc/clang
2025-12-23 Fixed multiple bugs:
           - Fix: //-comments in macros now stripped during definition (prep.c)
           - Fix: Switch statements now detect duplicate case values (stmt.c)
           - Fix: Local extern declarations cannot be initialized (decl.c)
           - Fix: Array indexing now works with char/short types (expr.c)
           - Fix: Unary '!' operator now rejects struct/union operands (expr.c)
           - Fix: char assignment now truncates result value (tree.c, gen.c)
           - Fix: printf %x/%o now treats values as unsigned (runtime/lib/vformat.c)
           - Fix: struct/union by value in function parameters now rejected (decl.c)
2025-12-23 Cleaned up sym.c:
           - Extracted namecmp() helper for repeated symbol name comparisons
           - Extracted emitdef() helper for duplicated data emission logic
           - Extracted isptr() helper for pointer type checking
           - Simplified objsize() using switch statement for base types
           - Reduced code duplication in defglob() and defloc()
2025-12-23 Improved -o flag and removed assembly file override restriction:
           - The -o flag now works with -S to specify custom assembly output filename
           - Removed restriction that prevented overwriting existing .s files
           - Updated help text to reflect -o works for both linker and assembly output
2025-12-23 Completed floating-point integration in expr.c, gen.c, tree.c:
           - Added FP operators to ops.h: OP_FLIT, OP_DLIT, OP_FADD/SUB/MUL/DIV,
             OP_DADD/SUB/MUL/DIV, OP_FNEG, OP_DNEG, OP_FEQ/NE/LT/GT/LE/GE,
             OP_DEQ/NE/LT/GT/LE/GE, OP_ITOF/ITOD/FTOI/DTOI/FTOD/DTOF
           - expr.c: Added FLOATLIT parsing in primary(), FP type support in
             typematch(), cast(), unary +/-, mkop() generates FP operators
           - gen.c: Added FP support in genstore(), genrval(), binopchk()
           - tree.c: Added FP operator emission (arithmetic, comparisons)
           - scan.c: Fixed 0.0 parsing (was incorrectly treated as octal)
           - defs.h: Increased NAMELEN from 16 to 32
           - Created comprehensive FP test suite: tests/fptest.c
           - All architectures generate correct FP code:
             * i386: x87 FPU (flds, fstps, faddp, fsubp, fmulp, fdivp)
             * x86-64: SSE2 (movss/sd, addss/sd, subss/sd, mulss/sd, divss/sd)
             * ARM: VFP (vldr, vstr, vadd.f32/f64, vsub, vmul, vdiv)
             * 8086: Software emulation (__fpadd, __fpsub, etc.) or x87
2025-12-23 Implemented floating-point code generation for all architectures:
           - i386: Full x87 FPU support (fld, fst, fadd, fsub, fmul, fdiv, etc.)
           - x86-64: Full SSE2 support (movss/sd, addss/sd, comiss/sd, etc.)
           - ARM: Full VFP support (vldr, vstr, vadd.f32/f64, vcmp, etc.)
           - 8086: Completed IEEE 754 double emulation in fpemu.c
             (comparison, arithmetic, int/float/double conversions)
2025-12-23 Simplified build system for cross-compilation:
           - Removed configure script (no longer needed)
           - Updated Makefile for direct cross-compiler build
           - Just run 'make' to build, 'make help' for options
           - No symlinks needed - all targets compiled into single binary
2025-12-23 Added floating-point arithmetic support to code generators:
           - New enum cg_fpu_type: FPU_NONE, FPU_X87, FPU_SSE, FPU_VFP, FPU_EMULATED
           - Added fpu_type field to cg_arch structure
           - Added CG_FPU_TYPE macro for runtime FPU type checking
           - Extended cg_vtable with 46 floating-point operation functions:
             * Load/Store: cgfloads, cgfloadd, cgfloadgs, cgfloadgd, cgfstores, etc.
             * Literals: cgflits, cgflitd
             * Arithmetic: cgfadds/d, cgfsubs/d, cgfmuls/d, cgfdivs/d, cgfnegs/d
             * Comparisons: cgfcmps/d, cgfeqs/d, cgfnes/d, cgflts/d, cgfgts/d, etc.
             * Conversions: cgitofs/d, cgftois/d, cgstod, cgdtos
             * Stack ops: cgfpush, cgfpop, cgfxch
             * Constants: cgdeffloat, cgdefdouble
           - 8086: Full x87 coprocessor support (8087/80287 instructions)
           - 8086: Software IEEE 754 emulation for systems without FPU
           - New target: dos-8086-x87 (uses 8087 coprocessor)
           - Updated dos-8086 target to use FP emulation by default
           - Created runtime/dos-8086/fpemu.c with IEEE 754 emulation library
           - i386: FPU_X87 (integrated FPU since 486)
           - x86-64: FPU_SSE (SSE2 standard on all x86-64)
           - ARM: FPU_VFP (VFP when available)
2025-12-23 Added full C89 type support:
           - New primitive types: short, long, unsigned variants, float, double
           - New keywords: SHORT, LONG, UNSIGNED, SIGNED, FLOAT, DOUBLE, CONST
           - Scanner updated to recognize floating-point literals with suffixes
           - Type specifier combinations: "unsigned int", "long int", "signed char", etc.
           - Architecture-specific type sizes in cg_arch structure:
             short_size, long_size, float_size, double_size
           - New code generation functions: cgdefh (2 bytes), cgdefd (4 bytes), cgdefq (8 bytes)
           - Type sizes now use CG_SHORTSIZE, CG_LONGSIZE, etc. macros
           - Correct code generation for all architectures:
             8086: short=dw(2), int=dw(2), long=dd(4)
             i386: short=.word(2), int=.long(4), long=.long(4)
             x86-64: short=.word(2), int=.quad(8), long=.quad(8)
2025-12-23 Increased compiler limits for better compatibility:
           - MAXFNARGS: 32 → 127 (max function parameters)
           - MAXCASE: 256 → 1024 (max cases in switch)
           - MAXBREAK: 16 → 64 (max nested loops/switches)
           - MAXLOCINIT: 32 → 128 (max local initializers)
2025-12-23 Added symbol_transform callback to cg_arch structure for custom symbol
           name transformation. This allows architectures like HLASM to implement
           their own symbol naming conventions (uppercase, 8-char limit, etc.).
           Modified gsym() in gen.c to use the callback when available.
           Removed hardcoded 'C' prefix - symbols now follow standard ABI:
           - ELF targets (Linux, FreeBSD, etc.): no prefix (main, printf)
           - Darwin/macOS: underscore prefix (_main, _printf)
           - Windows: underscore prefix (_main, _printf)
           Created cg_arch_x86_64_darwin and cg_arch_i386_windows with
           appropriate symbol_transform callbacks.
2025-12-23 Implemented ABI-compliant calling conventions:
           - x86-64: System V AMD64 ABI with args in rdi,rsi,rdx,rcx,r8,r9
           - ARM: AAPCS with args in r0,r1,r2,r3
           - i386/8086: cdecl with all args on stack
           Added cgpusharg(), cgcallprep(), cgcallend(), cgfnentry() to vtable.
           Function prologues now save register args to stack for access.
2025-12-23 Fixed ABI limitation: decl.c now uses architecture-specific fields
           for parameter and local variable offset calculation. Added
           CG_PARAM_OFFSET_BASE, CG_PARAM_OFFSET_DIR, CG_LOCAL_OFFSET_BASE,
           and CG_LOCAL_OFFSET_DIR macros. This enables STACK_UP architectures
           like IBM S/370 and z/Architecture.
2025-12-23 Added local_offset_dir and num_arg_regs fields to cg_arch structure.
2025-12-22 Added -I flag for multiple include directory support.
2025-12-22 Major refactoring by Alexandre Bencz: implemented cross-compilation support
           via vtable-based code generator dispatch. The compiler can now target
           multiple architectures from a single binary using the -T flag.
2025-12-22 Added -T <target> flag to select target architecture at runtime.
2025-12-22 Added -L flag to list all available targets.
2025-12-22 Created modular header structure: types.h, tokens.h, ops.h, cgen_compat.h.
2025-12-22 Implemented cgtarget.h/cgtarget.c with vtable-based architecture framework.
2025-12-22 Created cg_os_configs.c with OS-specific configurations.
2025-12-22 Reorganized code generators into src/targets/arch/<arch>/ directories:
           - i386/cg_i386.c
           - x86_64/cg_x86_64.c
           - 8086/cg_8086.c
           - arm/cg_arm.c
2025-12-22 Removed dependency on symlinks for target selection (cg.c, cg.h, sys.h).
2025-12-22 Removed platform-specific crt0 files (kept only DOS/8086 crt0).
2025-12-22 Updated version to 2025-12-22.
2022-01-23 Fixed a bug in struct declaration. Thanks, Paul Edwards!
2021-08-17 Changed type of jmp_buf, so that setjmp(jmp_buf) works (without &).
2021-08-17 Fixed array declarations in typedef struct.
2021-08-17 Added file name argument to #line preprocessor directive.
2021-08-16 Added support for anonymous structs.
2021-08-16 Added mixed struct and struct object declarations: struct {...} x;
2021-08-16 Cleaned up some typedef corner cases.
2021-08-16 Using typedef instead of #define in libc.
2021-08-16 Fixed a bug in malloc()'s defrag(). Thanks, Jean-Marc Lienher!
2021-08-15 Added a subset of the typedef statement and user-defined types.
2021-08-15 Fix: zero-size arrays are now invalid.
2021-08-13 Fix: removed invalid optimization x*2^n-->x<<n. Thanks, Bill Buzbee!
2016-12-12 Fix: char c=-2; stored -2, but char is unsigned. (also by Volkmar)
2016-12-01 Fix: f(a[1] && a[0]) would generate wrong code. Thanks, Volkmar!
2016-11-28 Fix: (a,1),2 would generate wrong code. (also by Volkmar)
2016-11-24 Fix: (x|1)|(x>1) would generate wrong code. (also by Volkmar)
2016-08-07 OpenBSD: not linking against crtbegin.o. Thanks, Volkmar Klatt!
2016-07-26 Added NetBSD/386 port supplied by Minux. Thanks!
2016-07-26 Applied signal() patch for NetBSD/x86-64. Thanks, Minux!
2016-02-17 Patch: added // comments. Thanks again, Steve!
2016-02-16 Fix: Darwin back-end emitted wrong asm code. Thanks, Steve Ward!
2016-02-16 Fix: SubC would miscompile x<256;x=0; (crash). Thanks, Quan Tran!
2016-02-16 Fix: freopen() had a precedence error. (also by Quan Tran)
2016-02-16 Fix: Constant folding miscalculated right-shift. (also by Quan Tran)
2015-02-18 Fix: Emitted wrong arguments to MULQ on x86 in struct* scaling.
2015-02-02 Fix: Long init lists in array declarations could run out of nodes.
2014-12-07 Fix: strncpy() should pad destination. Thanks, Alexey Frunze!
2014-12-07 Fix: fseek() should return 0 on success. (also by Alexey)
2014-12-07 Fix: longjmp() may not pass 0 to setjmp(). (also by Alexey)
2014-12-07 Fixed byte shift code emitted for x86. Thanks, Teran McKinney!
2014-06-22 Running extended triple test only in case of failure.
2014-06-03 Fix: syntax errors in constexprs could crash the compiler.
2014-05-25 Updated and re-organized documentation.
2014-05-24 Added operation reordering (tree skewing), e.g.: a+b*c --> b*c+a.
2014-05-24 Added strength reduction 2/2^N --> 2>>N.
2014-05-24 Added strength reduction 2*2^N --> 2<<N.
2014-05-20 Fix: convert '/' in all commands paths in Windows system().
2014-05-19 Applied patch for fixing system() on Windows. Thanks, Jean-Marc!
2014-05-18 Added OpenBSD ELF note.
2014-05-18 Fix: missed some functions when adapting OpenBSD crt0 to R-L calls.
2014-05-18 Applied more Windows patches by Jean-Marc Lienher. Thanks!
2014-05-17 Updated documentation.
2014-05-17 Changed random number test in libtest.c.
2014-05-17 Made kprintf() convert '\n' on descriptors 1,2 on DOS.
2014-05-16 Made some stdio functions emit CR,LF for '\n' on stdout/err on DOS.
2014-05-14 Added triple test script to gather data in case of failure.
2014-05-14 Added some DOS #ifdef:s to libtest.c (./file vs .\file).
2014-05-14 Adapted printf test in libtest.c for 16-bit systems.
2014-05-13 Fixed setjmp() function in DOS port (overwrote caller's memory).
2014-05-12 Updated documentation.
2014-05-10 Fix: added installation of (non-stdio) ncrt0.o to Makefile.
2014-05-10 Applied a load of Windows patches by Jean-Marc Lienher. Thanks!
2014-05-09 Fix: wrong Opt_sum_lim when BPW != sizeof(int).
2014-05-09 Removed constant expr parser, using constant folding instead.
2014-05-05 Increased NODEPOOLSZ.
2014-05-05 Windows port builds, but programs crash; back to EXPERIMENTAL.
2014-05-05 Changed winbuild.bat to print instructions, using make again.
2014-05-04 Changed triple test to use assembly output instead of binaries.
2014-05-04 Added -N (don't use stdio) flag and generation of ncrt0's.
2014-05-03 Removed "make" from winbuild.bat script (probably still broken).
2014-05-03 Fix: remove trailing '\r' in preprocessor lines.
2014-05-03 Gave up writing a signal(3) function for NetBSD. Using libc for now.
2014-05-03 Removed almost all libc dependencies from NetBSD-x86-64 crt0.
2014-05-02 Removed libc dependencies from FreeBSD-x86-64 crt0.
2014-05-01 Fixed order of argc/argv on DOS port (due to changed calling conv).
2014-05-01 Marked all untested ports "untested".
2014-05-01 Added removal of zero offsets from address computation.
2014-04-30 Adapted all remaining ports to new calling convention.
2014-04-29 Adapted NetBSD-x86-64 crt0 to new calling convention.
2014-04-29 Adapted FreeBSD-x86-64 crt0, build failed, marked BROKEN.
2014-04-28 Adapted FreeBSD-armv6 crt0 to new calling convention.
2014-04-28 Adapted FreeBSD-386 crt0 to new calling convention.
2014-04-28 Removed varargs redefinition hack (no longer needed).
2014-04-28 Changed calling convention to standard C (right-to-left).
2014-04-27 Added optimizer.
2014-04-26 Added tree abstraction.
2014-04-20 Forked this version.
2014-04-07 Fixed XSTRIP and SNM programs on 16-bit systems.
2014-04-05 Made s86 cache code for faster resolution of forward references.
2014-04-05 Fix: missing file name in error messages due to refactoring.
2014-04-03 Added DOS build script (targets/dos-8086/dosbuild.bat).
2014-04-02 Clean-up and misc. small fixes.
2014-04-01 The compiler can now be cross-compiled to DOS.
2014-04-01 Updated codumentation.
2014-04-01 Added DOS/EXE targets to Makefiles.
2014-03-31 Made compiler controller a separate executable on DOS.
2014-03-31 Factored out compiler controller.
2014-03-29 Documented bootstrapping of the DOS version.
2014-03-28 Added minimal runtime for compiling SCC86 on DOS.
2014-03-28 Added system() function for DOS.
2014-03-28 Added heap limit check to DOS _sbrk().
2014-03-27 Fix: struct_type[x] generated wrong offset.
2014-03-25 Turned __argc keyword into a local variable.
2014-03-23 Fix: #error could be mistakenly executed in negative #ifdef.
2014-03-22 Fix: fseek() could truncate files on DOS (zero-length _write()).
2014-03-22 Fix: OpenBSD sys.h file had wrong name.
2014-03-22 Changed default output file name for Windows.
2014-03-21 Fix: sizeof(void) was not an error.
2014-03-21 Made scc keep non-temporary object files.
2014-03-21 Added ufseek() (unsigned fseek) function to S86.
2014-03-20 Updated documentation.
2014-03-19 Both S86 and SLD run on 16-bit systems now.
2014-03-19 Made SLD store its relocation buffer on disk.
2014-03-19 Updated build script for windows. (untested!)
2014-03-18 Changed malloc() over-alloc. threshold for 16-bit systems.
2014-03-18 Made SLD store its relocation table on disk.
2014-03-17 Cleaned up the S86 tool chain, moved to s86/.
2014-03-17 Added setjmp() and longjmp() to DOS crt0.
2014-03-17 Made s86 write RELOC table to file to save memory.
2014-03-16 Added volatile keyword (as no-op).
2014-03-14 Misc. small fixes to libc (purely stylistic).
2014-03-14 Made printf("%s",NULL); print (NULL) instead of crash.
2014-03-14 Added missing CG prototypes.
2014-03-13 Added arena check to realloc().
2014-03-13 Added ad-hoc libc test suite. (tests/libtest.c)
2014-03-13 Implemented proper (unsigned) pointer comparison. (all back-ends)
2014-03-13 Fix: missing cmp queue commit in queue_cmp() (all back-ends).
2014-03-11 Fix: _vformat() crashed on unknown % sequences.
2014-03-11 Fix: limited octal escape sequences to three digits.
2014-03-11 Fix: added code to handle void values in expressions.
2014-03-11 Improved long usage (-d ?), misc cosmetical fixes.
2014-03-09 Updated misc. docs.
2014-03-06 Added code to fuse logic ops and conditional jumps (optimization).
2014-02-26 Fixed code generator bug in armv6 back end.
2014-02-26 Fixed alignment in galloc() for cross-compilation.
2014-02-25 Added vprintf(), vfprintf(), and vsprintf() functions.
2014-02-25 Added (slightly incompatible) varargs mechanism.
2014-02-23 Fixed duplicate register use in 8086 CG.
2014-02-21 Made s86 keep its code output on disk instead of in memory.
2014-02-20 Moved switch tables to code segment.
2014-02-19 Documented SLD (small loader) file formats.
2014-02-18 Fixed DOSRELOC bug in SLD.
2014-02-17 Successfully cross-compiled main(){printf("hi\n");} for DOS/8086.
2014-02-17 Fixed wrong DUP argument in S86.
2014-02-17 Fixed wrong return value in fread().
2014-02-16 Updated docs, misc. cosmetic changes.
2014-02-15 Added tests/sys.c program for testing crt0 functions.
2014-02-15 Fixed %p in *printf functions (wrong result for values < 0).
2014-02-14 Wrote simple crt0 test suite.
2014-02-14 Added fcntl.h include file, added SEEK_* constants to unistd.h.
2014-02-13 Removed system libc dependencies from freebsd-armv6 crt0.
2014-02-12 Removed system libc dependencies from freebsd-386 crt0.
2014-02-12 Re-organized targets/ directory, adapted configure script.
2014-02-07 Added FreeBSD/armv6 crt0 (crt0-freebsd-armv6.s).
2014-02-06 Added FreeBSD/armv6 back-end (cgarmv6.c).
2014-02-05 Added alignment option to galloc() for CPUs with aligned access.
2014-02-03 Fix: wait_bye should not be visible at C level in Darwin crt0.
2014-02-03 Added version info (-V option).
2014-02-02 Renamed cerror() due to libc conflict (FreeBSD 10.0-RC5). wtf?!
2014-01-19 Updated configure script.
2014-01-19 Removed old, stack-based back-end.
2014-01-15 Fixed alignment of environ and __progname in x86-64 crt0's.
2014-01-15 Added experimental support for Darwin/x86-64. Thanks, Romain LWPB!
2014-01-15 Made CG emit ".comm" instead of ".lcomm/.globl" for globals.
2014-01-14 Added OpenBSD/386 crt0 module (crt0-openbsd-386.s). Thanks, Romain!
2014-01-05 Removed superfluous system libc from linux sys.h.
2013-09-25 Fixed missing error messge in linker invocation.
2013-08-29 Added BUILD.BAT script for Windows by Jean-Marc Lienher. Thanks!
2013-05-24 Fixed a potential bug in the SYN/386 generator. Thanks, JM Lienher!
2013-05-21 Cleaned up the Windows code a bit (purely stylistic).
2013-05-18 Updated README, described Windows/MinGW port.
2013-05-18 Added FreeBSD voodoo section to 386 port. Thanks, Bakul Shah!
2013-05-17 Added Windows/386 support supplied by Jean-Marc Lienher. Thanks!
2013-02-28 Fix: fread() returned wrong length for partially filled buffers.
2013-02-27 Fix: setjmp()/longjmp() did not work properly.
2013-02-26 Fix: struct x { int *a, *b; }; generated int **b.
2013-02-21 Added a synthesizing back-end for the x86-64.
2013-02-19 Fix: *p+=n and *p-=n with int *p yielded wrong result.
2013-02-19 Fix: stdout was not line-buffered.
2013-02-18 Made printf %p output unsigned.
2013-02-18 Fixed wrong output of printf("%d", INT_MIN);
2013-02-18 Finished new code-synthesizing back-end.
2013-02-13 Added pointer arithmetics test suite.
2013-02-03 Reversed this file.
2013-02-03 Fixed some typos, updated book link in README.
2012-12-03 Added the linux x86-64 crt0 module supplied by "minux". Thanks!
2012-05-14 Made the compiler clean up *.o files after linking.
2012-05-14 Added -vvv option (also lists remove commands).
2012-05-14 Fix: reporting non-existent source files without .[sc] suffix.
2012-05-07 Fix: middle argument of ?: is an expr (not a logic op).
2012-04-27 Renamed syscall.h to unistd.h for portability.
2012-04-26 Removed all GNU libc dependencies from Linux crt0.
2012-04-25 Added the Linux/386 crt0 module (crt0-linux-386.s).
2012-04-18 Added a configure script.
2012-04-18 Moved machine word size to target description.
2012-04-17 Added the FreeBSD/x86-64 crt0 module (crt0-freebsd-x86-64.s).
2012-04-17 Improved some error messages.
2012-04-16 Fixed an off-by-one error in getenv().
2012-04-15 Re-organized archive to support multiple targets.
2012-04-15 Experimental NetBSD/x86 support completed.
2012-04-14 Added the NetBSD/x86-64 startup module (crt0-netbsd-x86-64.s).
2012-04-13 Added the x86-64 code generator (cgx86-64.c).
2012-04-12 Fixed increment/decrement of pointer to struct/union.
2012-04-05 Made structs/unions invalid operands to arithmetic ops.
2012-04-05 Added struct/union cast operators.
2012-04-04 Added proper struct* arithmetics for + and -.
2012-04-03 Added proper struct* arithmetics for ++ and --.
2012-04-03 Added #error, #line, #pragma preprocessor commands.
2012-04-03 Added the kprintf() function (fd-based printf()).
2012-04-03 Fixed pointer arithmetics (struct/union**).
2012-04-02 Made FILE a struct. (Breaking stdio library!)
2012-04-02 Fixed declaration parser (accept fn returning struct/union).
2012-04-01 Changed jmp_buf type from int[2] to struct _jmp_buf.
2012-03-31 Added struct/union access semantics.
2012-03-30 Added struct/union access syntax (x->y and x.y).
2012-03-30 Added local static struct/union definition semantics.
2012-03-29 Added local struct/union definition syntax and semantics.
2012-03-29 Added global struct/union definition syntax and semantics.
2012-03-28 Added struct/union symbol table support.
2012-03-27 Added global struct/union declaration syntax.
2012-03-26 Added static prototypes.
2012-03-26 Added local extern identifiers.
2012-03-26 Added local enum declarations.
2012-03-26 Added auto and register keywords (no-ops).
2012-03-26 Made "&array" valid syntax.
