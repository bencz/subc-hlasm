
	TODO:

	- fgetpos()/fsetpos() should support longer seek.

	- Add more target architectures:
		? RISC-V (rv32/rv64)
		? MIPS
		? PowerPC
		? IBM S/370 / z/Architecture (HLASM)

	- Implement runtime library cross-compilation support
	  (currently only code generation is cross-platform)

	- Fix functions with many parameters (32+) on AArch64
	  Stack parameter offset calculation may be incorrect

	- Fix infinite loop bug on certain valid C89 code
	  (t_array_complex.c, t_const_expr.c, t_global.c, t_state_machine.c)

	- Add block-scoped variable declarations (C89 feature)
