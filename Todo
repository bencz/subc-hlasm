
	TODO:

	- Fix the bugs in BUGS.

	- fgetpos()/fsetpos() should support longer seek.

	- Add more target architectures:
		? RISC-V (rv32/rv64)
		? MIPS
		? PowerPC
		? IBM S/370 / z/Architecture (HLASM)

	- Implement runtime library cross-compilation support
	  (currently only code generation is cross-platform)

	- Integrate floating-point code generation into expr.c/gen.c
	  (vtable functions implemented for all architectures)

	DONE (2025-12-23):
	- Implement FP code generation for i386 (x87), x86-64 (SSE2), ARM (VFP)
	- Complete double (64-bit) emulation in runtime/dos-8086/fpemu.c

	? clean up sym.c
